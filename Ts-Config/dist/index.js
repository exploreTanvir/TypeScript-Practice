"use strict";
console.log("This is index.ts");
const display = (d) => {
    let x = 25;
    console.log("their should be show some error for decleare parameter but not used and another error for decleare local variable but not used from index.ts ");
};
display();
